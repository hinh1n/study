请帮我在当前 Python RAG 项目中实现“第一阶段 RAG 入口控制”，目标是解决用户输入“嗨、你好、谢谢、在吗”等无知识库查询意图的内容时，系统仍然向量检索 RAG 库并把无关内容传给大模型的问题。

重要限制：
1. 不要改变当前连接大模型的逻辑。
2. 不要改变当前 embedding 模型的调用逻辑。
3. 不要替换当前向量库、embedding 模型或大模型 SDK。
4. 只在现有 RAG 流程前后增加轻量控制逻辑。
5. 保持改动尽量小，优先复用现有函数和项目结构。

请实现以下能力：

一、在进入向量检索前增加 should_use_rag(query) 判断。

逻辑要求：
1. 如果用户输入为空、只有空格、只有标点符号，则不使用 RAG。
2. 如果用户输入是明显问候语或闲聊短语，则不使用 RAG。
   例如：
   - 嗨
   - 你好
   - 您好
   - hi
   - hello
   - 哈喽
   - 在吗
   - 谢谢
   - 多谢
   - 再见
   - 拜拜
   - 早上好
   - 晚上好
3. 如果用户输入清洗后长度过短，例如小于等于 2 个中文字符或英文 token 太少，则不使用 RAG。
4. 其他情况保持原有 RAG 流程。

二、不要让这些不需要 RAG 的输入进入 embedding 和向量检索。

也就是说：
- should_use_rag(query) 返回 False 时：
  - 不调用 embedding
  - 不调用 vector_search
  - 不拼接知识库上下文
  - 直接走当前普通大模型回复逻辑

三、给向量检索结果增加最低相关性阈值过滤。

要求：
1. 保留当前向量检索逻辑。
2. 在拿到检索结果后，增加分数过滤。
3. 如果当前项目返回的是 similarity/cosine score，则只保留分数 >= threshold 的结果。
4. 如果当前项目返回的是 distance，则只保留 distance <= threshold 的结果。
5. threshold 做成配置项或常量，不要写死在多个地方。
6. 如果过滤后没有结果：
   - 不要把空或低相关知识库内容传给大模型。
   - 直接走普通大模型回复逻辑，或者按照当前项目已有的“无知识库结果”逻辑处理。

四、增加最小化的配置项。

例如：
- ENABLE_RAG_GATE=true
- RAG_MIN_SIMILARITY=0.72
- RAG_MAX_DISTANCE=0.35
- RAG_MIN_QUERY_LENGTH=3

如果项目已有配置系统，请接入现有配置系统；如果没有，则先用模块级常量，保持简单。

五、增加必要的日志。

请在以下位置增加 debug/info 日志：
1. query 被判断为不需要 RAG 时，记录 reason，例如 greeting、empty、too_short、punctuation_only。
2. 向量检索结果被阈值过滤后，记录过滤前数量和过滤后数量。
3. 因为没有高相关结果而跳过 RAG 时，记录原因。

六、请增加或更新测试。

至少覆盖这些场景：
1. “嗨” 不应调用 embedding，不应调用向量检索。
2. “你好” 不应调用 embedding，不应调用向量检索。
3. 空字符串不应调用 RAG。
4. 只有标点符号，比如 “？？？” 不应调用 RAG。
5. 正常知识库问题应该继续走原 RAG 流程。
6. 向量检索结果低于相似度阈值时，不应把知识库内容传给大模型。
7. 向量检索结果高于相似度阈值时，保持原 RAG 回答逻辑。

七、代码风格要求：
1. 保持项目原有代码风格。
2. 不要大范围重构。
3. 不要改现有 embedding、大模型连接、向量库初始化逻辑。
4. 不要引入新的大模型、rerank 模型或外部服务。
5. 如果需要新增函数，优先放在现有 RAG pipeline 附近，或新增一个轻量 util 文件。
6. 函数命名尽量清晰，例如：
   - should_use_rag(query) -> bool
   - get_rag_skip_reason(query) -> str | None
   - filter_retrieval_results_by_score(results)

期望最终流程变成：

用户输入
  ↓
should_use_rag(query)
  ├─ False：直接普通大模型回复，不调用 embedding，不查向量库
  └─ True：
       ↓
     保持当前 embedding 逻辑
       ↓
     保持当前向量检索逻辑
       ↓
     按相似度/distance 阈值过滤
       ↓
     如果没有高相关结果：普通大模型回复或走当前无结果逻辑
       ↓
     如果有高相关结果：保持当前 RAG + 大模型回答逻辑

请完成实现，并运行现有测试。如果没有测试框架，请至少提供可手动验证的测试用例和调用方式。
